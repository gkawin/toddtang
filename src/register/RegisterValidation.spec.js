import * as RegisterValidation from './RegisterValidation'

describe('RegisterValidation', () => {
  describe('assign error validation messages', () => {
    it('should contain `phone` `email` error messages', () => {

    })
  })

  describe('test rule by rule', () => {
    it('should be check both required and minimun allowed length', () => {})
  })
})
